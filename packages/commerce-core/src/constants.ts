/** Env var naming the active provider in a template, e.g. "shopify". */
export const COMMERCE_PROVIDER_ENV = "COMMERCE_PROVIDER";

/** Products tagged with this are hidden from storefront listings. A template
 * convention (inherited from Next.js Commerce), honored by provider mappers. */
export const HIDDEN_PRODUCT_TAG = "corte-frontend-hidden";

/** Variant title providers use for single-variant products. */
export const DEFAULT_OPTION = "Default Title";
